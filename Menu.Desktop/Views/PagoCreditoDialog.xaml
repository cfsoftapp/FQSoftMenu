<Window x:Class="Menu.Desktop.Views.PagoCreditoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Registrar pago"
        Width="500"
        Height="430"
        MinWidth="480"
        MinHeight="410"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="22" FontWeight="SemiBold" Text="Registrar pago" />

        <Border Grid.Row="1"
                Margin="0,14,0,16"
                Padding="12"
                Background="#EEF6FF"
                BorderBrush="#90CAF9"
                BorderThickness="1"
                CornerRadius="5">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="{Binding Comensal}" />
                <TextBlock Margin="0,6,0,0" FontSize="18" Text="{Binding TotalTexto}" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Margin="0,0,7,0">
                <TextBlock Margin="0,0,0,5" Text="Forma de pago" />
                <ComboBox Height="34"
                          DisplayMemberPath="Text"
                          ItemsSource="{Binding FormasPago}"
                          SelectedValue="{Binding FormaPago}"
                          SelectedValuePath="Value" />
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="7,0,0,0">
                <TextBlock Margin="0,0,0,5" Text="Fecha de pago" />
                <DatePicker Height="34" SelectedDate="{Binding FechaPago}" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="3" Margin="0,16,0,0">
            <TextBlock Margin="0,0,0,5" Text="Observacion" />
            <TextBox Height="38"
                     Padding="9,6"
                     MaxLength="200"
                     Text="{Binding Observacion, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel Grid.Row="5"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Width="120" Height="36" Click="Cancelar_Click" Content="CANCELAR" />
            <Button Width="140"
                    Height="36"
                    Margin="10,0,0,0"
                    Background="#00A86B"
                    BorderBrush="#00A86B"
                    Click="Confirmar_Click"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Content="CONFIRMAR PAGO" />
        </StackPanel>
    </Grid>
</Window>
