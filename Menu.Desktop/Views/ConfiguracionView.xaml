<UserControl x:Class="Menu.Desktop.Views.ConfiguracionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="Background" Value="#594AE2" />
            <Setter Property="BorderBrush" Value="#594AE2" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#594AE2" />
        </Style>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Margin" Value="2,0" />
            <Setter Property="Padding" Value="7,3" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D9DDE7" />
            <Setter Property="Foreground" Value="#594AE2" />
        </Style>
        <Style x:Key="FormPanel" TargetType="Border">
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Padding" Value="14" />
            <Setter Property="Background" Value="#F8F9FC" />
            <Setter Property="BorderBrush" Value="#DFE3EC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="5" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,14">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock FontSize="22" FontWeight="SemiBold" Text="Configuración" />
                <TextBlock Margin="0,3,0,0" Foreground="#777" Text="Parámetros compartidos por la versión web y escritorio." />
            </StackPanel>
            <Button DockPanel.Dock="Right" Command="{Binding RefreshCommand}" Content="ACTUALIZAR" Style="{StaticResource SecondaryButton}" />
        </DockPanel>

        <TabControl Grid.Row="1" Background="White" BorderBrush="#E1E4EC">
            <TabItem Header="Precio del menú">
                <Grid Margin="22">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="Precio vigente" />
                    <StackPanel Grid.Row="1" Margin="0,16,0,0" Orientation="Horizontal">
                        <StackPanel Width="220">
                            <TextBlock Margin="0,0,0,5" Text="Precio por menú (PEN)" />
                            <TextBox Height="36" Padding="8,6" Text="{Binding PrecioMenu, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <Button Margin="12,22,0,0" Command="{Binding SavePriceCommand}" Content="GUARDAR PRECIO" Style="{StaticResource PrimaryButton}" />
                    </StackPanel>
                    <TextBlock Grid.Row="2" Margin="0,14,0,0" Foreground="#777" Text="{Binding FechaActualizacion, StringFormat=Última actualización: {0}}" />
                </Grid>
            </TabItem>

            <TabItem Header="Tipos de personal">
                <Grid Margin="18">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <DockPanel Margin="0,0,0,12">
                        <TextBlock VerticalAlignment="Center" FontSize="17" FontWeight="SemiBold" Text="Tipos de personal" />
                        <Button DockPanel.Dock="Right" Command="{Binding NewTipoCommand}" Content="+ NUEVO" Style="{StaticResource PrimaryButton}" />
                    </DockPanel>
                    <Border Grid.Row="1" Style="{StaticResource FormPanel}" Visibility="{Binding ShowTipoForm, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.2*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,10,0">
                                <TextBlock Margin="0,0,0,4" Text="Nombre" />
                                <TextBox Height="34" Padding="8,5" Text="{Binding TipoNombre}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                <TextBlock Margin="0,0,0,4" Text="Descripción" />
                                <TextBox Height="34" Padding="8,5" Text="{Binding TipoDescripcion}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2" Margin="0,20,0,0" Orientation="Horizontal">
                                <Button Margin="0,0,8,0" Command="{Binding CancelTipoCommand}" Content="CANCELAR" Style="{StaticResource SecondaryButton}" />
                                <Button Command="{Binding SaveTipoCommand}" Content="GUARDAR" Style="{StaticResource PrimaryButton}" />
                            </StackPanel>
                        </Grid>
                    </Border>
                    <DataGrid Grid.Row="2" ItemsSource="{Binding TiposEmpleado}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" RowHeight="38">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="1.2*" Header="Nombre" Binding="{Binding Nombre}" />
                            <DataGridTextColumn Width="2*" Header="Descripción" Binding="{Binding Descripcion}" />
                            <DataGridCheckBoxColumn Width="90" Header="Activo" Binding="{Binding Activo}" />
                            <DataGridTemplateColumn Width="180" Header="Acciones">
                                <DataGridTemplateColumn.CellTemplate><DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Command="{Binding DataContext.EditTipoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Content="Editar" Style="{StaticResource ActionButton}" />
                                        <Button Command="{Binding DataContext.ToggleTipoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Content="Activar / desactivar" Style="{StaticResource ActionButton}" />
                                    </StackPanel>
                                </DataTemplate></DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Empresas cliente">
                <Grid Margin="18">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <DockPanel Margin="0,0,0,12">
                        <TextBlock VerticalAlignment="Center" FontSize="17" FontWeight="SemiBold" Text="Empresas cliente" />
                        <Button DockPanel.Dock="Right" Command="{Binding NewEmpresaCommand}" Content="+ NUEVA" Style="{StaticResource PrimaryButton}" />
                    </DockPanel>
                    <Border Grid.Row="1" Style="{StaticResource FormPanel}" Visibility="{Binding ShowEmpresaForm, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.2*" />
                                <ColumnDefinition Width="1.6*" />
                                <ColumnDefinition Width="0.8*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,10,0"><TextBlock Margin="0,0,0,4" Text="Nombre comercial" /><TextBox Height="34" Padding="8,5" Text="{Binding EmpresaNombre}" /></StackPanel>
                            <StackPanel Grid.Column="1" Margin="0,0,10,0"><TextBlock Margin="0,0,0,4" Text="Razón social" /><TextBox Height="34" Padding="8,5" Text="{Binding EmpresaRazonSocial}" /></StackPanel>
                            <StackPanel Grid.Column="2" Margin="0,0,10,0"><TextBlock Margin="0,0,0,4" Text="RUC" /><TextBox Height="34" Padding="8,5" Text="{Binding EmpresaRuc}" /></StackPanel>
                            <StackPanel Grid.Column="3" Margin="0,20,0,0" Orientation="Horizontal">
                                <Button Margin="0,0,8,0" Command="{Binding CancelEmpresaCommand}" Content="CANCELAR" Style="{StaticResource SecondaryButton}" />
                                <Button Command="{Binding SaveEmpresaCommand}" Content="GUARDAR" Style="{StaticResource PrimaryButton}" />
                            </StackPanel>
                        </Grid>
                    </Border>
                    <DataGrid Grid.Row="2" ItemsSource="{Binding Empresas}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" RowHeight="38">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="1.2*" Header="Nombre comercial" Binding="{Binding NombreComercial}" />
                            <DataGridTextColumn Width="1.7*" Header="Razón social" Binding="{Binding RazonSocial}" />
                            <DataGridTextColumn Width="110" Header="RUC" Binding="{Binding Ruc}" />
                            <DataGridCheckBoxColumn Width="75" Header="Activo" Binding="{Binding Activo}" />
                            <DataGridTemplateColumn Width="180" Header="Acciones">
                                <DataGridTemplateColumn.CellTemplate><DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Command="{Binding DataContext.EditEmpresaCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Content="Editar" Style="{StaticResource ActionButton}" />
                                        <Button Command="{Binding DataContext.ToggleEmpresaCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Content="Activar / desactivar" Style="{StaticResource ActionButton}" />
                                    </StackPanel>
                                </DataTemplate></DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Sucursales">
                <Grid Margin="18">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <DockPanel Margin="0,0,0,12">
                        <TextBlock VerticalAlignment="Center" FontSize="17" FontWeight="SemiBold" Text="Sucursales" />
                        <Button DockPanel.Dock="Right" Command="{Binding NewSucursalCommand}" Content="+ NUEVA" Style="{StaticResource PrimaryButton}" />
                    </DockPanel>
                    <Border Grid.Row="1" Style="{StaticResource FormPanel}" Visibility="{Binding ShowSucursalForm, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.1*" />
                                <ColumnDefinition Width="1.6*" />
                                <ColumnDefinition Width="1.3*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,10,0"><TextBlock Margin="0,0,0,4" Text="Nombre" /><TextBox Height="34" Padding="8,5" Text="{Binding SucursalNombre}" /></StackPanel>
                            <StackPanel Grid.Column="1" Margin="0,0,10,0"><TextBlock Margin="0,0,0,4" Text="Dirección" /><TextBox Height="34" Padding="8,5" Text="{Binding SucursalDireccion}" /></StackPanel>
                            <StackPanel Grid.Column="2" Margin="0,0,10,0"><TextBlock Margin="0,0,0,4" Text="Empresa cliente" /><ComboBox Height="34" ItemsSource="{Binding EmpresasActivas}" DisplayMemberPath="NombreComercial" SelectedValuePath="Id" SelectedValue="{Binding SucursalEmpresaId}" /></StackPanel>
                            <StackPanel Grid.Column="3" Margin="0,20,0,0" Orientation="Horizontal">
                                <Button Margin="0,0,8,0" Command="{Binding CancelSucursalCommand}" Content="CANCELAR" Style="{StaticResource SecondaryButton}" />
                                <Button Command="{Binding SaveSucursalCommand}" Content="GUARDAR" Style="{StaticResource PrimaryButton}" />
                            </StackPanel>
                        </Grid>
                    </Border>
                    <DataGrid Grid.Row="2" ItemsSource="{Binding Sucursales}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" RowHeight="38">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="1.2*" Header="Sucursal" Binding="{Binding Nombre}" />
                            <DataGridTextColumn Width="1.4*" Header="Empresa cliente" Binding="{Binding EmpresaCliente.NombreComercial}" />
                            <DataGridTextColumn Width="1.8*" Header="Dirección" Binding="{Binding Direccion}" />
                            <DataGridCheckBoxColumn Width="75" Header="Activo" Binding="{Binding Activo}" />
                            <DataGridTemplateColumn Width="180" Header="Acciones">
                                <DataGridTemplateColumn.CellTemplate><DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Command="{Binding DataContext.EditSucursalCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Content="Editar" Style="{StaticResource ActionButton}" />
                                        <Button Command="{Binding DataContext.ToggleSucursalCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Content="Activar / desactivar" Style="{StaticResource ActionButton}" />
                                    </StackPanel>
                                </DataTemplate></DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

        <TextBlock Grid.Row="2" Margin="2,10,0,0" Foreground="{Binding StatusColor}" Text="{Binding StatusMessage}" />
    </Grid>
</UserControl>
